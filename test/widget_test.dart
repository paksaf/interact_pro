// Smoke test stub. The original `flutter create`-generated test referenced
// a non-existent `MyApp` class — this file replaces it with a no-op so
// `flutter test` doesn't fail. Real widget tests should live in
// test/widget/<feature>_widget_test.dart.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — replace with real widget tests as features land', () {
    expect(1 + 1, 2);
  });
}
